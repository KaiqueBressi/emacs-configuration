import Component from '@ember/component';
import { reads } from '@ember/object/computed';

export default Component.extend({
  tagName: '',

  customer: reads('model.lead.customer'),

  fullName: reads('customer.fullName'),
  cpf: reads('customer.cpf'),

  didRender() {
    debugger;
  },
});
